{
  "name": "pi-delegate",
  "version": "0.1.0",
  "description": "Concurrent subagents for pi: the parent delegates a brief to a child session that runs in the background, and you can scope into any child to steer it. The child reports back through a single tool; nothing else it says reaches the parent.",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "files": ["index.ts", "src/**/*.ts", "skills/**/*.md", "README.md"],
  "keywords": ["pi-package"],
  "license": "MIT",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-agent-core": "0.85.1",
    "@earendil-works/pi-ai": "0.85.1",
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@earendil-works/pi-tui": "0.85.1",
    "@types/node": "^26.0.0",
    "bun-types": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
